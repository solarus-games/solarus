#ifndef ZSDX_SDL_USER_EVENTS_H
#define ZSDX_SDL_USER_EVENTS_H

// TODO: remove that file

/* SDL user event types
 * This enumeration is designed to make our own events
 * for the SDL loop but we don't have any ones for now.
 */
enum sdl_user_event_type_t {
  
};

#endif
